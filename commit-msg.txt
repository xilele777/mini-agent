feat(todo): add persistent task list with in-memory cache and startup hook

- 新增 add_todo / list_todos / remove_todo 三个工具，needsApproval: false
- 审批边界按「是否可能伤到不可撤销的用户资产」判定，而非「是否写盘」；todo 写死路径、只影响自身草稿本，因此免审批
- 状态收敛为模块私有 let state: Persisted，仅三个工具函数可访问，入口收窄
- 启动时由 initTools() 经注册表调用 loadTodoFromDisk；agent.ts 仍不 import 具体工具
- 读盘三态吞错：文件不存在 / JSON 损坏 / 结构不符 → 回空清单，不崩
- 写盘纪律：先 await writeFile 再 state = next，确保任意崩溃点内存与磁盘一致
- 持久化文件 .mini-agent-todo.json 放项目根并加入 .gitignore；list_todos 走内存，免 IO
- 注册到 ALL_TOOLS，npm run typecheck 干净
- 实测：跨进程重启恢复、跨轮记忆、运行中手改文件不跟变/重启生效、删除文件优雅回空
- 已知限制：多进程并发写会覆盖；todo 拆完不会自动逐条执行，留待执行循环课