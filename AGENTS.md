# mini-agent 开发协作

这是一个通过亲手编写 TypeScript CLI Agent 学习 Agent 原理的项目。AI 按本项目选择的教练工作流协作。

## 开始工作

- 涉及教学、实现指导、代码 review 或学习交接时，阅读 [learning-coach](.agents/skills/learning-coach/SKILL.md)。
- 学习背景和编辑约定见 [项目学习配置](.agents/learning/project.md)；当前进度与证据见 [学习进度](.agents/learning/progress.md)。
- 根据本次请求选择需要的内容，不在每次小问题中重读全部历史。用户当前要求决定任务范围，明确授权可覆盖默认的教学分工。

## 开发决策

非平凡的行为、架构、协议、跨模块约定或测试策略变更，使用 [write-notes-like-deepseek](.agents/skills/write-notes-like-deepseek/SKILL.md)。

- 修改前按模块名或机制名检索 `.agents/notes/`，日常检索排除 `archived/`。笔记中的源码路径也是检索入口。
- 优先维护已有决定。新取舍用 `proposed`；已落地事实用 `implemented`；有防止重犯价值的否决理由用 `rejected`。普通格式、错别字修改不新建笔记。
- 每篇说明问题、决定、真实备选及代价；验证结论必须有相应证据。学习进度不写成决策状态。
- 历史补录以最早可核实的记录日期归位，并注明日期证据与实际整理日期；没有依据时使用整理日期，不猜测更早的讨论时间。备选取舍不冒充已经做过的对比实验。
- 本项目补录既有决定时，通过笔记中的源码链接和本入口的检索规则建立关联；遵守学习者维护源码的约定，不为补录笔记单独修改 `src/`。学习者以后修改相关入口时，可增加源码反向注释。
- 已确认的决定及已授权的文档维护直接执行，不重复索要确认；尚未确定的实质性选择再向用户澄清。
- 上游 Skill 保持原样。项目约定写在本文件或学习配置中，通用教学方法写在独立的 `learning-coach` 中。

## 验证与交接

- 决策笔记变更运行 `npm run verify-notes`。
- 源码相关验证使用 `npm run typecheck` 和适合改动的测试；全量测试入口为 `npm test`。
- 交接时在学习进度中区分：代码状态、理解证据、运行验证和待验证项；只更新确有变化的内容。
- `docs/HANDOFF.md` 是本地详细学习记录，可能不随 Git 克隆存在。缺少它时依据已跟踪的学习配置、进度和实际源码继续。
- 安装来源、维护方式和迁移步骤见 [开发协作说明](.agents/README.md)。
