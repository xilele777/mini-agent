# 运费边界修复任务

业务规则：订单商品金额满 100 元免运费，不足 100 元收取 10 元运费。输入已由调用方保证为非负有限数，本任务不增加输入校验。

目前边界测试失败。请先阅读代码和测试，修复 `src/shipping.ts`，执行测试，并根据实际结果总结。

只修改 `src/shipping.ts`；不要修改测试、package.json 或本说明，也不要安装依赖。所有依赖复用 mini-agent 根目录的安装。

这是固定评测模板，实际任务必须在 `npm run fixture:new` 创建的副本中进行。原始模板预期为两项通过、一项失败；修复后预期三项通过。
